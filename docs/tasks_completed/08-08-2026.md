# Task Completed: 08-08-2026

## Task: Add "View Profile" Buttons to Requests & Offers Tab
**Date:** August 8th, 2026

### What Was Done
Added a "View Profile" functionality to the **Requests & Offers** section for both the Teacher and Student dashboards. This allows users to view the profile of the person they are negotiating with or receiving requests from.

### Implementation Details
1. **Teacher Dashboard (`web/src/app/dashboard/teacher/page.tsx`)**:
   - Injected a "View Profile" button into the request cards.
   - Tied the `onClick` event to find the student from `data?.allStudents` using the request's `groupDocId` or `studentDocId`.
   - Reused the existing modal by setting `setSelectedViewUser` to the fetched student data and `setSelectedViewApp` to the current request (`neg`).

2. **Student Dashboard (`web/src/app/dashboard/student/page.tsx`)**:
   - Added a matching "View Profile" button into the tutor request cards.
   - Tied the `onClick` event to find the teacher from `data?.allTutors` using `neg.tutorDocId`.
   - Populated the existing modal by setting `setSelectedViewUser` to the tutor data and `setSelectedViewApp` to the current request (`neg`).

3. **Privacy Logic Preservation**:
   - Verified that the existing view modal (around line 2641 in the teacher dashboard) relies on `selectedViewApp.status` to determine whether contact details should be visible.
   - By passing the current request to `setSelectedViewApp`, the modal correctly applied the rule: Contact details (like phone numbers and email) are hidden with a placeholder message until the status reaches `demo_booked`, `tuition_started`, or another approved state.

---

## Task: Verify Demo & Hiring Request Restrictions
**Date:** August 8th, 2026

### What Was Done
Conducted a codebase analysis to determine if rules surrounding single active demos and auto-rejecting waitlisted tutors were already in place.

### Implementation Details
1. **Only One Demo at a Time**:
   - Verified that `student/page.tsx` already uses `isLockedByOtherTutor` to check if *any* application for a group is in a demo phase (`demo_booking_phase`, `demo_scheduled`, `waiting_for_parent_decision`).
   - If true, all other requests display a disabled button: **"Waitlisted: Demo in progress with another teacher."**
   
2. **Global Locks for Teachers**:
   - Verified that `teacher/page.tsx` relies on `data.globalLocks` to enforce the same rule on the tutor's end, rendering a locked "Waitlisted" UI for other tutors while a student is in a demo process.

3. **Auto-Rejecting Others When Hired**:
   - Verified that `handleAppointTutor` in `student/page.tsx` runs when a parent hires a tutor (`tuition_started`).
   - This function successfully loops through all other active applications for that group, automatically updates them to `status: 'declined'` with the reason `student_hired_another_tutor`, and sends an in-app notification to the declined teachers stating: *"This student has hired another tutor"*.
   
**Result:** No code changes were needed because the requested logic was already completely implemented.

---

## Task: Verify Demo Date/Time Negotiation Loop
**Date:** August 8th, 2026

### What Was Done
Analyzed the codebase to confirm if an option exists for both parents and teachers to continuously update and propose a demo date/time after fee payment until a mutual agreement is reached.

### Implementation Details
1. **Initial Proposal Loop**:
   - After payment (`status: demo_booking_phase`), the teacher is prompted with a **"Propose Date & Time"** button in `teacher/page.tsx`.
   - Submitting this proposal updates the request's `proposedDate`, `proposedTime`, and sets `lastUpdatedBy = 'teacher'`.
   
2. **Counter-Proposal System (Ping-Pong Effect)**:
   - Once a date is proposed by one party, the other party's UI automatically shifts to display two buttons: **"Accept Proposed Date"** and **"Change requested Date/Time"**.
   - If the student clicks "Change", they submit a counter-proposal which switches the UI back so the teacher must now decide to accept or counter-propose again.
   - This back-and-forth relies on flipping the `lastUpdatedBy` variable continuously.

3. **Final Confirmation**:
   - The loop successfully terminates when either party clicks **"Accept Proposed Date"**.
   - The `handleNegotiationAction` automatically catches this click, switches the status to `demo_scheduled`, locks the finalized date, and ends the negotiation phase.

---

## Task: Verify Paying Tutor Waitlist Edge Case
**Date:** August 8th, 2026

### What Was Done
Analyzed the codebase to ensure that the tutor who actually pays the demo fee is *not* incorrectly shown the "Waitlisted" UI lock that competing tutors see.

### Implementation Details
1. **Tutor ID Verification**:
   - Verified that `teacher/page.tsx` strictly verifies identity before locking the UI: `lockInfo.tutorDocId !== data?.user?.uid`.
   - Because of this, the active tutor who owns the lock (made the payment) seamlessly bypasses the waitlist screen and accesses the active scheduling UI.
   - Competing tutors naturally fail this check and are correctly presented with the disabled "Waitlisted" UI block.

**Result:** No code changes were needed. The back-and-forth scheduling functionality and edge-case identity checks are working perfectly as required in the current system.

---

## Task: Real-Time Queue Verification & Auto-Cleanup
**Date:** August 8th, 2026

### What Was Done
Refactored the application queue logic to verify a teacher's pending request count directly against the database in real-time, preventing the "ghost application" bug caused by out-of-sync local arrays (e.g., when an admin manually deletes an application).

### Implementation Details
1. **Real-Time Verification**:
   - Updated `handleRequestTutor` and `handleDirectRequestDemo` in `student/page.tsx`.
   - Updated `handleSendOffer` and `handleDirectRequestDemo` in `teacher/page.tsx`.
   - Before executing a request, the logic now slices the local `pendingRequests` array and uses the `where(documentId(), 'in', reqIds)` operator to fetch the exact documents from the `applications` Firebase collection.
   - The system then actively counts only the requests that actually exist *and* have an active status, ensuring 100% queue count accuracy before enforcing the limit logic.

2. **Self-Healing Cleanup Mechanism**:
   - During the database fetch, the code compares the fetched results against the local array.
   - Any IDs in the local array that are missing from the database (or are no longer active) are categorized as "orphaned".
   - The system immediately triggers a background `updateDoc` on the teacher's profile, using `arrayRemove(...orphanedIds)` to seamlessly wipe the bad data.
   - This creates a self-healing environment where any desynced teacher profile will automatically correct itself the next time a request is attempted.

---

## Task: Custom Display IDs Implementation Documentation
**Date:** August 8th, 2026

### What Was Done
Integrated custom display IDs (`parentId`, `groupId`, `studentId`, `tutorId`, `applicationId`) across the Student and Teacher portals to improve legibility and context. Fixed issues with IDs not bubbling up from dynamically grouped student arrays in the frontend.

### Implementation Details
1. **Student / Parent Portal (`student/page.tsx`)**:
   - **Profile Hover Dropdown**: Injected the `parentId` directly beneath the parent's email address. Styled identically to the Gmail hover dropdown for subtlety (`text-xs text-gray-500 truncate`).
   - **Parent Profile Banner**: Displayed the `parentId` on the far right side of the teal profile banner inside a sleek, semi-transparent box (`bg-white/10 px-3 py-1 rounded-lg border`).
   - **Group Details Modal Title**: The modal header now displays the group ID alongside the title: `Group 1: Student Details (ID: MTG-XXX)`. *Technical Note*: Because the frontend dynamically groups student documents into arrays, the `groupId` is lost at the root object level. The ID is correctly extracted by safely querying the first student in the nested array: `viewingGroupDetails.students[0]?.groupId`.
   - **Individual Student Cards (Inside Group Modal)**: Each student's specific `studentId` is displayed directly below their name (`text-xs text-slate-500 font-medium`).
   - **Requests & Offers Cards**: Displayed the `applicationId` prominently on all application cards. The font size was bumped up to `text-xs` for optimal readability.
   - **View Teacher Profile Modal**: When a parent views a teacher's full profile, the `tutorId` is proudly displayed right next to the teacher's name in a readable `text-sm` font.

2. **Teacher Portal (`teacher/page.tsx` & `TeacherForm.tsx`)**:
   - **Profile Hover Dropdown**: Mirroring the student portal, the `tutorId` was injected directly beneath the email address (`text-xs text-gray-500 truncate`).
   - **Teacher Profile Banner (`TeacherForm.tsx`)**: Injected the `tutorId` on the far right side of the primary teal profile banner using the exact same semi-transparent styling as the Parent Profile. 
   - **Requests & Offers Cards**: Displayed the `applicationId` on all incoming and outgoing application cards (`text-xs`).
   - **View Student Profile Modal**: When a teacher clicks to view a New Tuition lead, the modal displays **both** the `Parent ID` and the `Group ID` side-by-side beneath the parent's name. *Technical Note*: Similar to the student portal, the teacher portal dynamically groups incoming leads. To reliably fetch the custom IDs, the modal digs into the nested student array: `selectedViewUser.students[0]?.parentId` and `selectedViewUser.students[0]?.groupId`.

3. **Styling Standards Applied**:
   Across all implementations, strict design consistency was maintained:
   - **Primary Banner IDs**: `text-lg font-bold text-white font-mono bg-white/10 px-3 py-1 rounded-lg border border-white/20`
   - **Dropdown IDs**: `text-xs text-gray-500 truncate`
   - **Modal Badge IDs**: `text-sm font-mono font-bold uppercase tracking-wider bg-black/10 px-2 py-1 rounded-md`
   - **Card IDs**: `text-xs text-slate-500`
